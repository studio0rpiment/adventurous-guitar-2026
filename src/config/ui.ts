/**
 * Which title treatment renders: the DOM overlay ("dom") or 3D text in the R3F
 * scene ("3d"). Flip back to "dom" to restore the overlay version — both are
 * kept.
 */
export const TITLE_MODE: "dom" | "3d" = "3d";
